language: node_js
node_js:
- 4.2.2
install:
- node make.js pack

after_success:
- echo $TRAVIS_BUILD_DIR
- curl --retry 10 --ftp-create-dirs -T $TRAVIS_BUILD_DIR/README.md -u $FTP_USER:$FTP_PASSWORD ftp://$FTP_HOST/temp/

branches:
  only:
  - develop
  - feature/163
